/**
 * ScrollView Native Wrapper
 *
 * Per iOS e Android usa lo ScrollView nativo di React Native
 * che funziona perfettamente su queste piattaforme
 */

import { ScrollView } from 'react-native';

// Riesposta direttamente lo ScrollView di React Native per le piattaforme native
export default ScrollView;
